# 🛡️ أدوات التشخيص والاختبار - Ransomware Defense Kit

## 📁 الملفات المتوفرة

تم إنشاء مجموعة كاملة من أدوات التشخيص والاختبار:

### 🔧 أدوات التشخيص

| الملف | الوظيفة | الاستخدام |
|-------|---------|-----------|
| `diagnostic_check.py` | فحص شامل للنظام | `python diagnostic_check.py` |
| `test_monitoring_debug.py` | اختبار watchdog مباشرة | `python test_monitoring_debug.py` |
| `quick_test.py` | إنشاء ملفات اختبار سريعة | `python quick_test.py` |
| `test_all.bat` | قائمة تفاعلية لكل الاختبارات | `test_all.bat` |

### 📚 ملفات التوثيق

| الملف | المحتوى |
|-------|----------|
| `DETECTION_FIX.md` | دليل إصلاح الكشف (إنجليزي) |
| `TROUBLESHOOTING_AR.md` | دليل استكشاف الأخطاء (عربي) |
| `QUICK_START_AR.md` | دليل البداية السريعة (عربي) |
| `README_TOOLS.md` | هذا الملف |

---

## 🚀 البداية السريعة (30 ثانية)

### الخيار 1: القائمة التفاعلية (أسهل!)

```cmd
test_all.bat
```

اختر الخيار المناسب من القائمة!

### الخيار 2: التشخيص اليدوي

```powershell
python diagnostic_check.py
```

سيخبرك بالضبط ما المشكلة وكيف تحلها.

---

## 📋 شرح مفصل لكل أداة

### 1️⃣ diagnostic_check.py

**الغرض:** فحص شامل للنظام والمتطلبات

**ماذا يفحص:**
- ✅ إصدار Python (3.7+)
- ✅ المكتبات المطلوبة (watchdog, PyQt5, psutil)
- ✅ هيكل المشروع (الملفات والمجلدات)
- ✅ ملف الإعدادات (config.json)
- ✅ مجلد السجلات (logs/)
- ✅ وظائف watchdog
- ✅ مجلد الاختبار

**النتيجة:**
```
============================================================
  SUMMARY
============================================================
✅ PASS - Python Version
✅ PASS - Dependencies
✅ PASS - Project Structure
...
Score: 7/7 checks passed

🎉 All checks passed! Your system is ready.
```

**إذا فشل شيء:**
سيعطيك تعليمات دقيقة للإصلاح!

---

### 2️⃣ test_monitoring_debug.py

**الغرض:** اختبار watchdog بشكل مباشر بدون الواجهة

**كيفية الاستخدام:**

```powershell
# Terminal 1
python test_monitoring_debug.py
# اتركه يعمل...
```

في PowerShell جديد:

```powershell
# Terminal 2
cd c:\Users\(-_\Pictures\RansomwareDefenseKit2
python test_ransomware_behavior.py
```

**ماذا يجب أن تراه:**
```
✓ CREATED: C:\Test_Quarantine\document_0.txt
🔄 RENAMED: C:\Test_Quarantine\document_0.txt → document_0.txt.locked
   🚨 SUSPICIOUS EXTENSION DETECTED!
```

**إذا لم تر أحداث:**
- ❌ watchdog غير مثبت → `pip install watchdog`
- ❌ مشكلة صلاحيات → Run as Administrator
- ❌ watchdog معطل → راجع الكود

---

### 3️⃣ quick_test.py

**الغرض:** إنشاء سيناريوهات اختبار بسرعة

**ماذا ينشئ:**
1. ملف عادي (document.txt)
2. إعادة تسمية بامتداد مشبوه (.locked)
3. ملف فدية (README.txt)
4. 10 ملفات سريعة (اختبار الانفجار)

**الاستخدام:**
```powershell
python quick_test.py
```

**النتيجة:**
```
✓ Test directory: C:\Quick_Test
✓ Created: document.txt
✓ Renamed: document.txt → document.txt.locked
   🚨 This should trigger CRITICAL alert!
✓ Created: README.txt
   🚨 This should trigger CRITICAL alert!
```

**ثم:**
1. شغّل البرنامج الرئيسي
2. أضف `C:\Quick_Test` للمراقبة
3. اضغط START MONITORING
4. راقب الأحداث!

---

### 4️⃣ test_all.bat

**الغرض:** قائمة تفاعلية لكل الاختبارات

**الخيارات:**

```
[1] Run Diagnostic Check       - فحص النظام
[2] Test Watchdog Monitoring   - اختبار المراقبة
[3] Quick Test                 - إنشاء ملفات اختبار
[4] Run Behavior Simulator     - محاكي السلوك الكامل
[5] Start Main Application     - البرنامج الرئيسي
[6] Full Test                  - كل الخطوات تلقائياً
```

**الخيار 6 (Full Test) يقوم بـ:**
1. تشغيل التشخيص
2. إنشاء ملفات اختبار
3. بدء مراقب watchdog في نافذة جديدة
4. تشغيل محاكي السلوك في نافذة جديدة
5. بدء البرنامج الرئيسي

**كل شيء تلقائي!** 🚀

---

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: "البرنامج لا يعمل أبداً"

```powershell
python diagnostic_check.py
```

سيخبرك بالضبط ما المشكلة.

---

### السيناريو 2: "watchdog لا يلتقط الأحداث"

```powershell
# Terminal 1
python test_monitoring_debug.py

# Terminal 2
python quick_test.py
```

إذا لم تظهر أحداث في Terminal 1:
- watchdog معطل
- مشكلة صلاحيات
- Python قديم

---

### السيناريو 3: "الأحداث تظهر لكن لا تطابق القواعد"

1. شغّل `test_monitoring_debug.py` - يظهر أحداث ✅
2. شغّل `main.py` - لا يظهر كشف ❌

**المشكلة:** القواعد في الكود!

**افحص:**
- هل القواعد معرّفة؟
- هل الامتدادات صحيحة؟
- هل العتبات منطقية؟

راجع ملف القواعد أو الكود.

---

### السيناريو 4: "أريد اختبار شامل"

```cmd
test_all.bat
```

اختر `[6] Full Test`

سيفتح 3 نوافذ:
- Watchdog Monitor
- Behavior Simulator  
- Main Application

راقب الأحداث تنتقل بينهم!

---

## 🔍 تفسير النتائج

### نتيجة ناجحة ✅

**diagnostic_check.py:**
```
Score: 7/7 checks passed
🎉 All checks passed!
```

**test_monitoring_debug.py:**
```
✓ CREATED: file.txt
🔄 RENAMED: file.txt → file.txt.locked
   🚨 SUSPICIOUS EXTENSION DETECTED!
```

**main.py:**
- Live Events يعرض الأحداث
- Severity بالألوان الصحيحة
- السجلات تُكتب في logs/

---

### نتيجة فاشلة ❌

**diagnostic_check.py:**
```
❌ FAIL - Dependencies
   PyQt5 not installed
   watchdog not installed
```

**الحل:**
```powershell
pip install PyQt5 watchdog
```

---

**test_monitoring_debug.py:**
```
Monitoring C:\Test_Debug_Monitor...
(لا شيء يحدث)
```

**الأسباب:**
1. watchdog غير مثبت
2. صلاحيات خاطئة
3. المجلد غير موجود

**الحل:**
```powershell
pip install watchdog
# ثم Run as Administrator
```

---

## 🆘 استكشاف الأخطاء السريع

| المشكلة | الاختبار | الحل |
|---------|----------|------|
| البرنامج لا يعمل | `diagnostic_check.py` | اتبع التعليمات |
| لا أحداث | `test_monitoring_debug.py` | تحقق من watchdog |
| القواعد لا تطابق | `quick_test.py` | راجع الكود |
| كل شيء بطيء | `test_all.bat [1]` | تحقق من النظام |

---

## 📚 ترتيب القراءة الموصى به

1. **ابدأ هنا:** `QUICK_START_AR.md`
2. **للمشاكل:** `TROUBLESHOOTING_AR.md`
3. **للإصلاح:** `DETECTION_FIX.md`
4. **الأدوات:** `README_TOOLS.md` (هذا الملف)

---

## ✅ قائمة التحقق السريعة

قبل أن تسأل "لماذا لا يعمل؟"

- [ ] شغّلت `diagnostic_check.py`
- [ ] كل الفحوصات 7/7 نجحت
- [ ] `test_monitoring_debug.py` يظهر أحداث
- [ ] `quick_test.py` أنشأ ملفات
- [ ] `main.py` يعمل بدون أخطاء
- [ ] START MONITORING نشط
- [ ] المجلد مضاف في Settings
- [ ] Run as Administrator

إذا كانت كل الإجابات نعم - يجب أن يعمل!

---

## 🎓 نصائح متقدمة

### تسجيل Debug

أضف في `main.py`:

```python
import logging
logging.basicConfig(
    level=logging.DEBUG,
    filename='debug.log',
    format='%(asctime)s - %(levelname)s - %(message)s'
)
```

ثم افحص `debug.log` للأخطاء.

---

### مراقبة الأداء

راقب استخدام CPU:

```python
import psutil
process = psutil.Process()
print(f"CPU: {process.cpu_percent()}%")
print(f"Memory: {process.memory_info().rss / 1024 / 1024:.1f} MB")
```

---

### اختبار مخصص

انسخ `quick_test.py` وعدّله لسيناريوهاتك!

---

## 🎉 الخلاصة

**لديك الآن:**

✅ 4 أدوات اختبار قوية  
✅ 4 ملفات توثيق شاملة  
✅ قائمة تفاعلية (test_all.bat)  
✅ أمثلة واضحة لكل سيناريو  

**ابدأ دائماً بـ:**

```powershell
python diagnostic_check.py
```

**أو:**

```cmd
test_all.bat
```

**ثم اتبع التعليمات!** 🚀

---

*تم الإنشاء: ديسمبر 2025 | Ransomware Defense Kit v1.0*
